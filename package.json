{
  "name": "muito_manga_api",
  "version": "1.0.0",
  "description": "A webscrapping API for the Muito Mangá website",
  "main": "index.js",
  "engines": {
    "node": ">=v16.15.1"
  },
  "scripts": {
    "start": "node dist/index.js",
    "postinstall": "tsc",
    "watch-node": "nodemon dist/index.js",
    "watch-ts": "tsc -w",
    "deploy": "git add . && git commit -m Heroku && git push heroku master",
    "start:dev": "nodemon",
    "clean:cache": "yarn cache clean --force && rm -rf node_modules && rm -rf yarn.lock",
    "build:tsc": "tsc"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.27.2",
    "cheerio": "^1.0.0-rc.12",
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "nodemon": "^2.0.19",
    "ts-node": "^10.8.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.12",
    "@types/express": "^4.17.13"
  }
}